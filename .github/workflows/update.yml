name: Actualizador de Datos Dinámico
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # ... (el resto de pasos se quedan igual)
      - name: Guardar cambios
        run: |
          git config --global user.name 'Cerebro-Bot'
          git config --global user.email 'bot@ivanplayer.com'
          git add calendar.json standings.json results.json
          git commit -m "Auto-update: $(date)" || exit 0
          git push
